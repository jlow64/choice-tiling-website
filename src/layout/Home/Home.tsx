import { HomeBody, HomeDescription, HomeTitle } from './Home.theme';

const Home = () => {
  return (
    <HomeBody>
      <HomeDescription>
        Experienced for over 10+ years in Tiling.
      </HomeDescription>
      <HomeTitle>We know Tiling.</HomeTitle>
    </HomeBody>
  );
};

export default Home;
